'use client';
import { useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';

export default function Signup(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),agency:f.get('agency'),email:f.get('email'),password:f.get('password')})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Could not create workspace.');router.push('/dashboard');router.refresh();}catch(x){setError(x.message)}finally{setBusy(false)}}return <main className="auth-wrap"><section className="auth-art"><div className="brand"><img className="brand-logo" src="/xzrecruiter-logo.png" alt="XZ Recruiter" width="205" height="30" /></div><div><h1>Turn hiring activity into agency revenue.</h1><p>Start with one niche. XZRecruiter will help your team see which companies deserve attention and why.</p></div><small className="muted">14-day pilot · No card required</small></section><section className="auth-main"><div className="form-card"><h2>Create your workspace</h2><div className="muted">Set up your agency in under two minutes.</div><form className="form" onSubmit={submit}>{error&&<div className="form-error">{error}</div>}<div className="field"><label>Your name</label><input name="name" autoComplete="name" required/></div><div className="field"><label>Agency name</label><input name="agency" autoComplete="organization" required/></div><div className="field"><label>Work email</label><input name="email" type="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" minLength="12" autoComplete="new-password" required/></div><button className="btn primary" disabled={busy}>{busy?'Creating workspace…':'Create workspace →'}</button><div className="form-note">Already have an account? <Link href="/login">Sign in</Link></div></form></div></section></main>}
