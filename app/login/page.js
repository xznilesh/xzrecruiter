'use client';
import { useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';

export default function Login(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Sign in failed.');router.push('/dashboard');router.refresh();}catch(x){setError(x.message)}finally{setBusy(false)}}return <main className="auth-wrap"><section className="auth-art"><div className="brand"><img className="brand-logo" src="/xzrecruiter-logo.png" alt="XZ Recruiter" width="205" height="30" /></div><div><h1>Start the day with the right accounts.</h1><p>Your ranked hiring radar, recruiter pipeline and agency intelligence — in one workspace.</p></div><small className="muted">XZRecruiter · Secure agency workspace</small></section><section className="auth-main"><div className="form-card"><h2>Welcome back</h2><div className="muted">Sign in to your agency workspace.</div><form className="form" onSubmit={submit}>{error&&<div className="form-error">{error}</div>}<div className="field"><label>Email</label><input name="email" type="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" autoComplete="current-password" required/></div><button className="btn primary" disabled={busy}>{busy?'Signing in…':'Sign in →'}</button><div className="form-note">New to XZRecruiter? <Link href="/signup">Create a free workspace</Link></div></form></div></section></main>}
