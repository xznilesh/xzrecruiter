import Link from 'next/link';
import AppShell from '@/app/components/AppShell';
import { requireReadyWorkspace } from '@/lib/workspace-ready';
import { sessionToken } from '@/lib/auth';
import { rpc } from '@/lib/supabase-api';
import { formatDateTime } from '@/lib/globalization';

export const dynamic='force-dynamic';

export default async function Dashboard(){
 const {user,globalContext}=await requireReadyWorkspace();
 const token=await sessionToken();let d={metrics:{companies:0,jobs:0,hot:0,clients:0,candidates:0},signals:[],pipeline:[]};
 try{const result=await rpc('xzrecruiter_dashboard',{p_token:token});if(result?.ok)d=result}catch(error){console.error('dashboard_rpc_failed',error?.message||'')}
 const settings=globalContext.settings;const m=d.metrics||{};const signals=Array.isArray(d.signals)?d.signals:[];const pipeline=Array.isArray(d.pipeline)?d.pipeline:[];const max=Math.max(1,...pipeline.map((x)=>Number(x.n)||0));const localNow=formatDateTime(new Date(),{locale:settings.locale||'en-GB',timezone:settings.timezone_id||'UTC'});
 return <AppShell user={user} globalSettings={settings} active="home"><div className="page-heading dashboard-heading"><div><span className="page-kicker">Hiring command center</span><h1>Good to see you, {user.display_name||'Recruiter'}.</h1><p>Run recruitment operations and hiring intelligence without losing market or timezone context.</p></div><div className="heading-statuses"><span className="status good">● Workspace ready</span><span className="status neutral">{settings.country_name} · {settings.timezone_id}</span></div></div><div className="fast-action-strip"><div><b>Fast-action workspace</b><span>Candidate → Job → Interview → Offer → Placement.</span></div><Link href="/candidates" className="small-action">＋ Candidate</Link><Link href="/jobs" className="small-action">＋ Job</Link><Link href="/pipeline" className="small-action">Pipeline</Link></div><div className="dashmetrics premium-metrics"><div className="dmetric"><span>Tracked companies</span><b>{m.companies||0}</b><small>market coverage</small></div><div className="dmetric"><span>Active jobs</span><b>{m.jobs||0}</b><small>hiring demand</small></div><div className="dmetric"><span>Hot accounts</span><b>{m.hot||0}</b><small>priority outreach</small></div><div className="dmetric"><span>Candidates</span><b>{m.candidates||0}</b><small>workspace talent</small></div></div><div className="global-context-card"><div><span className="eyebrow-mini">Viewer context</span><h2>{settings.country_name}</h2><p>{localNow}</p></div><dl><div><dt>Locale</dt><dd>{settings.locale}</dd></div><div><dt>Currency</dt><dd>{settings.currency_code}</dd></div><div><dt>Timezone</dt><dd>{settings.timezone_id}</dd></div><div><dt>Language</dt><dd>{settings.language_code?.toUpperCase()}</dd></div></dl><Link href="/settings/global">Change operating context →</Link></div><div className="panels premium-panels"><div className="panel"><div className="panel-title-row"><div><h2>Today’s highest-priority accounts</h2><div className="panel-sub">Hiring Heat combines freshness, urgency, trust and agency fit.</div></div><span className="panel-badge">Live intelligence</span></div>{signals.length?<div className="table"><div className="tr head"><div>Company</div><div>Heat</div><div>Fit</div><div>Recommendation</div></div>{signals.map((s,i)=><div className="tr" key={i}><div><b>{s.name}</b><div className="row-sub">{s.why_now_summary}</div></div><div className="heat">{s.heat_score}</div><div>{s.fit_score??'—'}%</div><div><span className="pill">{s.recommendation}</span></div></div>)}</div>:<div className="empty"><b>Hiring Intelligence is ready for later signal ingestion.</b><span>Your ATS is operational now; live company scoring remains a dedicated intelligence step.</span></div>}</div><div className="panel"><h2>Recruitment pipeline</h2><div className="panel-sub">Persistent candidate-to-job stage distribution.</div>{pipeline.length?<div className="bars">{pipeline.map((p)=><div className="barline" key={p.stage}><span>{p.stage}</span><div className="bar"><i style={{width:`${Math.max(6,(Number(p.n)||0)/max*100)}%`}}/></div><b>{p.n}</b></div>)}</div>:<div className="empty"><b>Start with a candidate and a job.</b><span>Create both, then add the candidate to the recruitment pipeline.</span><Link href="/pipeline" className="small-action">Open pipeline</Link></div>}</div></div></AppShell>
}
